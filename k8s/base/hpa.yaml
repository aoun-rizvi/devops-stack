apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: startup-api
  namespace: devops-stack
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: startup-api
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
